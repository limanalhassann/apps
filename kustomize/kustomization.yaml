apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - hpa.yaml
  - deployment.yaml
  - service.yaml

patches:
  - target:
      kind: Deployment
      name: podinfo
    path: patch.yaml